# Synthetic Claude usage preview

These screenshots render the actual provider-usage widget inside a labeled synthetic sidebar frame. Before shows a quota window without paid metadata; after adds a synthetic paid-usage response. They do not show a real account or a complete live Cafe session.

The video mounts the real widget, applies the synthetic paid record, and clicks Refresh against a mocked RPC. No provider process, account usage request, or credit redemption occurs. The temporary capture fixture is excluded from the branch.
